# frozen_string_literal: true

RSpec.describe Enginn do
  it 'has a version number' do
    expect(Enginn::VERSION).not_to be nil
  end

  # it 'does something useful' do
  #   expect(false).to eq(true)
  # end
end
